
  <%# if users.any? %>
    <!--ul class="list-unstyled" -->
      <% users.each do |user| %>
        <div class="media mt-3 mt-lg-5 col-sm-6 align-items-center">
          <% if user.image? %>
            <img class = "rounded-circle" src="<%= user.image %>" width=100 height=100>
          <% else %>
            <img class = "rounded-circle" src="/assets/default.jpg" width=100 height=100>
          <% end %>
          <div class="media-body ml-4">
            <div><%= user.name %></div>
            <div class="mt-2 small">　</div>
          </div>
        </div>
      <% end %>
    <!--/ul-->
  <%# end %>
