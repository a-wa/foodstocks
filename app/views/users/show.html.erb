<div class="row">
  <aside class="col-sm-4">
    <div class="card">
      <div class="card-header">
        <h3 class="card-title"><%= @user.name %></h3>
      </div>
      <div class="card-body">
        
      </div>
    </div>
  </aside>
  <div class="col-sm-8">
    <% unless current_user.family_id %>
      <%= render 'families/new_family', user: @user %>
    <% else %>
      <%= render 'index', users: @users %>
    <% end %>
    
  </div>
</div>