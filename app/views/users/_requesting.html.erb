<div class="mt-5">
    <% if current_user.requestings.any? %>
      <h5>招待中のユーザ</h5>
      <ul class="list-unstyled">
        <% guests.each do |guest| %>
          <li><%= guest.email %></li>
        <% end %>
      </ul>
    <% else %>
      <p>招待中のユーザはいません</p>
    <% end %>
  
</div>